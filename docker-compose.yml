version: '3.9'

services:
  web:
    build: .
    container_name: exposaas
    restart: always
    ports:
      - "3001:3001"
      - "5000:5000"
    env_file:
      - .env.docker     
    environment:
      NODE_ENV: production
    extra_hosts:
      - "host.docker.internal:host-gateway"
